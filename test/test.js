var assert = require('assert');
var $ = require('jquery');

describe('Addition', function() {
  describe('of integers', function() {
    it('should add two and two', function() {
      assert.equal(2 + 2, 4);
    });
  });
});

describe('Signup', function() {
  describe('for pet owners', function() {
    it('should allow pet owners to sign up', function() {

    });
  });
});
